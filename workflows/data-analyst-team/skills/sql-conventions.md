Always explore before querying: list tables first, then read the schema of
every table you intend to touch — the foreign keys ARE the JOIN rules, never
guess a join. Cite the executed query with your answer. Aggregate in SQL,
not in prose.
