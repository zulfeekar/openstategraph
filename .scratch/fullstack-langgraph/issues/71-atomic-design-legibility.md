Type: task
Status: resolved (2026-08-08)

## Question

Brutal-honest verdict on "Atoms/Molecules/Organisms/Templates/Pages is
applied throughout the ecosystem": **the structure exists de facto, the
mental model is legible nowhere.** Prebuilts ARE atoms, tier presets ARE
molecules, canvas patterns ARE organisms — but no code, doc, palette
grouping, or UI ever uses those words; the vocabulary lives only in a note
on ticket 32. A contributor cannot discover the model; that fails the
claim's spirit.

Fix: (1) an openwiki concept page teaching the mapping with the real
examples; (2) palette sections named by the model (Atoms: tools/middleware
· Molecules: agent tiers, router, grader · Organisms: Team, supervisor
pattern); (3) ticket 32's Inspector effective-stack panel labels each slot's
kind. Cheap, purely legibility — the architecture already conforms.

## Resolution

Wiki page `openwiki/architecture/atomic-design.md` (the five levels with real examples + the two holding rules), palette sections labelled with their level (Agents · molecules / Tools · atoms). Inspector slot labelling remains with ticket 32's panel.
